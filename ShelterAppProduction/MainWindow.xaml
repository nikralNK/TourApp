<Window x:Class="ShelterAppProduction.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShelterAppProduction"
        mc:Ignorable="d"
        Title="Приют для животных" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource LightBeige}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryGreen}" Padding="10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="Каталог" Click="CatalogButton_Click" Margin="5"/>
                    <Button Content="Избранное" Click="FavoritesButton_Click" Margin="5"/>
                    <Button Content="Профиль" Click="ProfileButton_Click" Margin="5"/>
                    <Button x:Name="AdminButton" Content="Админ-панель" Click="AdminButton_Click" Margin="5" Visibility="Collapsed" Background="Red" Width="150" Height="40"/>
                    <Button x:Name="VeterinarianButton" Content="Ветеринары" Click="VeterinarianButton_Click" Margin="5" Visibility="Collapsed" Background="Orange" Width="150" Height="40"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="UserNameTextBlock"
                              Foreground="White"
                              VerticalAlignment="Center"
                              Margin="10,0"/>
                    <Button Content="Выход" Click="LogoutButton_Click" Margin="5"/>
                </StackPanel>
            </Grid>
        </Border>

        <Frame x:Name="MainFrame"
               Grid.Row="1"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
